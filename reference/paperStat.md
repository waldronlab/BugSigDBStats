# Paper stats

Paper stats

## Usage

``` r
paperStat(dat, col)
```

## Arguments

- dat:

  a `data.frame` storing BugSigDB data

- col:

  character. A column of `dat` that should be tabled.

## Value

A sorted table with paper stats for the chosen metadata column
