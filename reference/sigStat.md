# Signature stats

Signature stats

## Usage

``` r
sigStat(dat, col)
```

## Arguments

- dat:

  a `data.frame` storing BugSigDB data

- col:

  character. A column of `dat` that should be tabled.

## Value

A sorted table with signature stats for the chosen metadata column
