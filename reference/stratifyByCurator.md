# Stratify curation output by curator

Stratify curation output by curator

## Usage

``` r
stratifyByCurator(dat)
```

## Arguments

- dat:

  a `data.frame` storing BugSigDB data

## Value

a numeric `matrix` storing number of papers and number of signatures
curated for each curator
